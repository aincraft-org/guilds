package dev.mintychochip.territory.model;

/** Settlement facility categories exposed to storage and shop integrations. */
public enum FacilityType {
    /** A facility for trading goods. */
    TRADING_POST,
    /** A facility for storing goods. */
    STORAGE,
    /** A facility used as a travel waypoint. */
    WAYSTONE
}
